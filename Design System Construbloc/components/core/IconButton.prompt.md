Square icon-only button for nav (menu, search, cart, close).
```jsx
<IconButton label="Menu" variant="inverse"><i data-lucide="menu"></i></IconButton>
```
Variants ghost/solid/accent/inverse; `size` px (default 40, min 44 on mobile).
